.container
  .col-sm-10.col-sm-offset-1.col-xs-12.blog-content
    - @projects.each do |project|
      .col-xs-12
        .text-center
          h2
            = project.title
          h6
            .date= project.funding_deadline
          = raw(project.description).truncate(358)
        .text-center
          = link_to "READ MORE", project_path(project)
        br
